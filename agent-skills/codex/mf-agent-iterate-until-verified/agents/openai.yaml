interface:
  display_name: "Iterate Until Verified"
  short_description: "Run independent quality loops on any task"
  default_prompt: "Use $mf-agent-iterate-until-verified to execute this task with independent quality gates and evidence."
